import { TimerStatePipe } from './timer-state.pipe';

describe('TimerStatePipe', () => {
  it('create an instance', () => {
    const pipe = new TimerStatePipe();
    expect(pipe).toBeTruthy();
  });
});
